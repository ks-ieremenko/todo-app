import { Checkbox, styled } from "@mui/material";

const StyledCheckbox = styled(Checkbox)({
  "&.MuiCheckbox-root.Mui-checked": {
    color: "rgba(99,63,231,0.66)",
  },
});
export default StyledCheckbox;
